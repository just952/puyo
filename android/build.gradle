apply plugin: 'com.android.application'

android {
    namespace 'com.puyo.game'
    compileSdk 33

    defaultConfig {
        applicationId "com.puyo.game"
        minSdk 21
        targetSdk 33
        versionCode 1
        versionName "1.0"

        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a'
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    sourceSets {
        main {
            assets.srcDirs = ['src/main/assets', '../assets']
            jniLibs.srcDirs = ['src/main/jniLibs']
        }
    }
}

dependencies {
    implementation project(':core')
    implementation "com.badlogicgames.gdx:gdx-backend-android:$gdxVersion"
    runtimeOnly "com.badlogicgames.gdx:gdx-platform:$gdxVersion:natives-armeabi-v7a"
    runtimeOnly "com.badlogicgames.gdx:gdx-platform:$gdxVersion:natives-arm64-v8a"
}

// Copy native .so files from runtime dependencies to jniLibs for packaging
task copyNativeLibs(type: Copy) {
    from configurations.runtimeOnly
    include "**/*.so"
    into "src/main/jniLibs"
}

// Ensure native libs are packaged before any build by attaching to preBuild
preBuild.dependsOn(copyNativeLibs)