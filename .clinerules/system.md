- 중간 분석 과정이나 혼잣말의 표시는 생략할 것. 
- 로컬환경이 termux 라 Android APK 빌드가 안되므로 github workflow 를 이용해 빌드하도록 환경 설정을 유지할 것.
- 로컬환경에서는 native lib 없으므로 단위 테스트 시도하지 말 것. 
- 로컬환경에서는 aapt2 미작동하므로 Android 빌드도 시도하지 말 것.
- native library 들도 로컬에서는 받으려 하지말고 github workflow 로 빌드시에만 메이븐 리파지토리에서 다운받도록 할 것.
- 하나의 Task를 처리할 때 먼저 작은 단위로 계획을 세우고 나에게 제시할 것. 
- ACT 모드에서도 작업 완료후 자동 커밋은 하지 말 것. 
